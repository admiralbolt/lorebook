import formatErrors from 'client/utils/format-errors';
import { module, test } from 'qunit';

module('Unit | Utility | format-errors', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = formatErrors();
    assert.ok(result);
  });
});
